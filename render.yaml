services:
  - type: web
    name: ai-order-agent
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    postdeployCommand: "playwright install chromium"
